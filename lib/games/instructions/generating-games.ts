export const generatingGames = `# Generating games

When you build or change a game, produce the complete \`index.html\` for it.

- Write clean, readable HTML, CSS, and JavaScript. Use \`<canvas>\` for
  action/arcade games and the DOM for puzzle/turn-based games as appropriate.
- Make the game immediately playable: include the controls, a clear objective,
  a game loop, win/lose states, and a way to restart.
- Keep it responsive so it looks right inside the preview pane, and make the
  play area scale to the available space.
- Handle input cleanly (keyboard, mouse, and touch where it makes sense) and
  avoid blocking the main thread.
- Favor small, understandable code over cleverness so the game is easy to
  iterate on in later turns.`
